# Test

This repository contains a simple example React component.

- `app.jsx` demonstrates how to fetch user data once on mount using
  `useEffect` to prevent repeated requests whenever the UI updates.
